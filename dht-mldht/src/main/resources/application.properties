# Spring Boot配置
spring.application.name=dht-mldht
spring.main.web-application-type=none

# DHT网络配置
dht.start-port=6881
dht.node-count=20
dht.distribute-node-ids=true
dht.bootstrap-nodes=router.bittorrent.com:6881,dht.transmissionbt.com:6881,router.utorrent.com:6881

# Spring Kafka配置
spring.kafka.bootstrap-servers=localhost:9092
spring.kafka.producer.key-serializer=org.apache.kafka.common.serialization.StringSerializer
spring.kafka.producer.value-serializer=org.springframework.kafka.support.serializer.JsonSerializer
spring.kafka.producer.acks=1
spring.kafka.producer.batch-size=16384
spring.kafka.producer.properties.linger.ms=10

# Kafka主题
kafka.topic.infohash=dht.infohash.discovered

# Spring Redis配置
spring.data.redis.host=localhost
spring.data.redis.port=6380
spring.data.redis.password=
spring.data.redis.database=0
spring.data.redis.timeout=3000ms

# 去重配置
dedup.enabled=true
dedup.ttl.days=7

# 性能配置
worker.threads=4
queue.size=10000
publish.batch.size=100

# 日志配置
logging.level.root=INFO
logging.level.cn.lihongjie.dht=DEBUG
logging.file.name=logs/mldht.log
